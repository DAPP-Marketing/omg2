# AGENTS.md

AIコーディングツール（Claude / Codex / ChatGPT など）が、**OMG株式会社 コーポレートサイト**で作業する際のルールです。
人間の保守担当者も、このルールに沿って作業してください。

## 現行サイト方針（2026年9月更新・最優先）

本サイトはユーザー指示により、採用特化サイトから **OMG株式会社のコーポレートサイト** へ変更しました。
事業紹介・会社案内・企業としての信頼性・お問い合わせを主目的とし、採用情報は補助コンテンツとして扱います。
以下に残る採用サイト向けルールは、技術構成、色、可読性、アクセシビリティ、データ分離、アニメーション制限など、コーポレートサイトと矛盾しない範囲で適用してください。

本サイトの主目的は、会社案内ではなく **採用・応募獲得** です。
梅プランでは、竹プランよりもページ数・コンテンツ量・アニメーションを絞り、**必要な情報を短く・分かりやすく・迷わず応募できる構成**を優先します。

---

## 0. 最重要の前提

本サイトは、**実務経験の少ない担当者でもAIコーディングツールを使って安全に更新できること**を前提とします。

判断に迷ったら、常に次を基準にします。

```text
どちらが初心者に理解しやすいか
どちらが変更箇所を探しやすいか
どちらが壊れにくいか
どちらが採用情報を更新しやすいか
どちらが実装コストを抑えられるか
```

> **「高度で綺麗なコード」より「初心者でも安全に変更できるコード」**

梅プランでは特に、機能や演出を増やしすぎないことを重視します。

---

## 1. このサイトの目的

### 最優先の目的

**OMG株式会社への応募数を増やすこと。**

梅プランでは、求職者が短時間で以下を判断できることを重視します。

1. どんな会社か
2. どんな仕事があるか
3. どんな雰囲気か
4. 条件・待遇はどうか
5. どう応募するか

### サイト全体の印象

- アットホーム
- 親しみやすい
- 明るい
- 安心感がある
- 人の温度を感じる
- 物流会社としての清潔感・信頼感がある

竹プランと同じデザイン品質を保ちつつ、**情報量と制作工数を抑える**のが梅プランです。

---

## 2. 梅プランの考え方

梅プランは、**「まず採用HPを持つ」ためのシンプル採用サイト**です。

### 竹プランとの違い

梅プランでは、以下を基本的に削減・簡略化します。

- 社員インタビューの詳細ページ
- 職種ごとの詳細ページ
- 1日の流れの専用ページ
- 職場ギャラリーの専用ページ
- 複雑なアニメーション
- カウントアップなどの演出
- 大量の下層ページ

ただし、以下は削減しません。

- デザイン品質
- スマホ対応
- 応募導線
- 読みやすさ
- 表示速度
- SEOの基本設定
- アクセシビリティ

---

## 3. デザイン方針（固定）

### メインカラー

```text
Primary Red: #E01B21
```

この赤を、以下に使用します。

- CTAボタン
- 強調見出し
- アイコン
- 下線
- 数字・アクセント

### ベースカラー

- 白：メイン背景
- 明るい空色：背景・グラデーション
- 濃紺：本文・見出し・ナビゲーション
- 薄いグレー：カード背景・罫線

濃紺や空色の具体値は、実装時にデザイントークンとして一箇所にまとめます。

### デザインの方向性

- 明るく余白がある
- 人物写真を大きく見せる
- 白ベース
- 大きく読みやすい見出し
- 丸みのあるカード・ボタン
- ゆるやかな波形または曲線の区切り
- 写真と短いコピーを中心に構成

### 禁止する表現

- **文字を斜めに配置しない**
- 本文を回転させない
- 読みにくい装飾文字を多用しない
- 写真の上に長文を重ねない
- 赤を背景全面に多用しない
- 細すぎる文字・小さすぎる文字を使わない
- 装飾目的だけのアニメーションを増やさない

---

## 4. 文字サイズ・タイポグラフィ

求人サイトなので、デザイン性より**読みやすさ**を優先します。

### PCの目安

```text
FVメインコピー: 64〜88px
セクション大見出し: 48〜64px
セクション小見出し: 28〜36px
カード見出し: 22〜28px
本文: 16〜18px
補足・ラベル: 13〜15px
```

### スマートフォンの目安

```text
FVメインコピー: 40〜56px
セクション大見出し: 32〜42px
セクション小見出し: 22〜28px
カード見出し: 20〜24px
本文: 15〜17px
補足・ラベル: 12〜14px
```

### ルール

- 行間は詰めすぎない
- 1行を長くしすぎない
- 長文は左揃えを基本とする
- CTAは一目で読める太さと大きさにする
- 竹プランより文字量を増やさない

---

## 5. TOPページ構成（梅プラン・固定）

TOPページだけで、求職者が応募判断に必要な最低限の情報を確認できる構成にします。

```text
TOP
├─ FV
│  └─ 「いい仲間と、いい仕事を。」
│
├─ OMGについて
│  └─ 会社・雰囲気の短い紹介
│
├─ OMGで働く魅力
│  ├─ アットホームな職場
│  ├─ 未経験でも安心
│  └─ 資格取得支援
│
├─ 仕事紹介
│  ├─ ドライバー
│  └─ 倉庫スタッフ
│
├─ 募集要項
│  └─ 給与・勤務時間・休日などの概要
│
├─ よくある質問
│  └─ 主要3〜5問程度
│
└─ ENTRY
   └─ 応募CTA
```

### TOPの原則

- 説明しすぎない
- 短いコピー + 写真 + 要点で構成する
- 詳細は募集要項ページへ集約する
- セクションを増やしすぎない
- 竹プランのコンテンツをそのまま縮小表示しない

---

## 6. 下層ページ構成（梅プラン・固定）

梅プランでは、下層ページを必要最低限にします。

```text
/company/
├─ 会社概要
├─ 代表メッセージ
└─ 拠点情報

/recruit/
├─ 募集職種
├─ 仕事内容
├─ 給与・待遇
├─ 勤務時間・休日
├─ 福利厚生
└─ 選考フロー

/entry/
├─ 応募フォーム
└─ お問い合わせ
```

必要に応じて `/faq/` を独立させても構いませんが、TOP内で完結できる場合は増やしません。

URL構成は明確な指示なしに変更しません。

---

## 7. FVの扱い

FVは本サイトの最重要エリアです。

### 基本構成

- OMGロゴ
- グローバルナビゲーション
- 応募するボタン
- 青空
- OMGのトラック
- 笑顔のスタッフ複数名
- メインコピー
- 短いサブコピー
- スクロール誘導

### コピーの基準

```text
いい仲間と、
いい仕事を。

走るのは、モノだけじゃない。
つながる、ひとの想いも。
```

FVと同程度の**少ない文字量**を、TOPの他セクションでも意識してください。

---

## 8. 作業前に必ず守ること

- **既存仕様を勝手に変更しない**
- 変更前に、既存コードの設計・命名・ディレクトリ構造を確認する
- 変更範囲を必要最小限にする
- 依頼されていないファイルを整形・並べ替え・書き換えしない
- 新しいライブラリを勝手に追加しない
- Primary Red `#E01B21` を変更しない
- TOPのセクション順を勝手に変更しない
- 応募CTAを削除・弱体化しない
- 梅プランの範囲を超える機能を勝手に追加しない

---

## 9. 技術構成（固定）

| 分類 | 使用技術 |
| --- | --- |
| フレームワーク | Astro |
| CSS | Tailwind CSS v4 |
| 言語 | TypeScript |
| ホスティング | Cloudflare Workers |
| 構成方針 | Static First |

### ルール

- Astro標準機能を優先する
- Tailwind CSS v4を優先する
- Cloudflare標準機能を優先する
- 不要なJavaScriptを増やさない
- 静的コンテンツを無理にJavaScript化しない
- 表示速度を重視する
- GSAPなど大型アニメーションライブラリは使用しない

---

## 10. 設計の優先順位

```text
可読性 > 保守性 > 安全性 > 表示速度 > 実装コスト > 再利用性 > 拡張性
```

- YAGNIを徹底する
- 同じロジックのコピーは避ける
- 似ているだけで無理に共通化しない
- 過度な抽象化をしない
- 予防的なRepository / Service / Factory / Adapter / Interfaceは作らない

---

## 11. コンポーネント設計

```text
1コンポーネント = 1つの役割
```

### 良い名前

```text
Header
Footer
Hero
AboutSection
BenefitsSection
JobsSection
RecruitSection
FaqSection
EntryCTA
JobCard
```

### 悪い名前

```text
Common
Parts
Component1
Box
Item
Data
Util
```

TOPの大きなセクションは、基本的に1セクション1コンポーネントとします。

---

## 12. ディレクトリ構成

```text
src/
├── components/
│   ├── common/
│   │   ├── Header.astro
│   │   ├── Footer.astro
│   │   └── EntryCTA.astro
│   │
│   ├── home/
│   │   ├── Hero.astro
│   │   ├── AboutSection.astro
│   │   ├── BenefitsSection.astro
│   │   ├── JobsSection.astro
│   │   ├── RecruitSection.astro
│   │   └── FaqSection.astro
│   │
│   └── recruit/
│
├── layouts/
│   └── BaseLayout.astro
│
├── pages/
│   ├── index.astro
│   ├── company/
│   ├── recruit/
│   └── entry/
│
├── data/
│   ├── company.ts
│   ├── navigation.ts
│   ├── benefits.ts
│   ├── jobs.ts
│   ├── recruit.ts
│   └── faq.ts
│
├── lib/
│   └── validation/
│
├── styles/
│   └── global.css
│
└── assets/
    ├── images/
    └── icons/
```

梅プランではディレクトリを増やしすぎません。
必要になっていない `people/` `day/` `workplace/` などを先回りで作成しないでください。

---

## 13. データとUIを分離する

変更頻度の高い情報は、Astroファイルへ直接書かず `src/data/` にまとめます。

### 主要データ

- `company.ts`
  - 会社名
  - 代表者
  - 所在地
  - 電話番号
  - 設立
  - 従業員数
  - 車両台数
  - 拠点

- `benefits.ts`
  - OMGで働く魅力

- `jobs.ts`
  - 職種名
  - 仕事内容
  - 必要資格
  - 募集状態

- `recruit.ts`
  - 雇用形態
  - 給与
  - 勤務時間
  - 休日
  - 福利厚生
  - 応募資格
  - 選考フロー

- `faq.ts`
  - よくある質問

### 特に注意する情報

以下は変更されやすいため、必ずデータファイル側で管理してください。

- 従業員数
- 車両台数
- 給与
- 休日
- 勤務時間
- 募集職種
- 応募資格
- 電話番号
- 拠点

複数ページへ直接重複記述しないでください。

---

## 14. 写真の扱い

梅プランでも、写真は採用成果に大きく影響します。

### 優先する写真

1. 笑顔の社員が複数人写っている写真
2. 仕事中の自然な表情
3. トラックと社員が一緒に写っている写真
4. 倉庫スタッフの作業風景
5. 車両・倉庫・設備

### 避ける写真

- 無人のトラックだけが続く
- 暗い倉庫
- 威圧感のある表情
- 不自然なポーズ
- 同じ人物・同じ構図ばかり

### 実装ルール

- `alt` を必ず設定する
- 適切なサイズへ最適化する
- FV以外は原則 lazy load
- レイアウトシフトを防ぐため画像サイズまたはアスペクト比を指定する
- 不要な大量画像を読み込まない

---

## 15. CTA・応募導線

採用サイトなので、応募CTAは最重要です。

### 基本文言

```text
応募する
募集要項を見る
仕事内容を見る
```

### ルール

- 「応募する」はPrimary Red `#E01B21` の目立つボタンにする
- ヘッダーに応募ボタンを置く
- TOP最下部にENTRYセクションを置く
- 募集要項ページにも応募CTAを置く
- ボタン文言を曖昧にしない
- 「詳しくはこちら」だけのボタンは避ける

---

## 16. 応募フォーム

問い合わせフォームではなく、**採用応募フォーム**を中心に設計します。

### 想定項目

- 氏名
- ふりがな
- 電話番号
- メールアドレス
- 希望職種
- 保有免許・資格
- 希望連絡方法
- 自由記入欄
- 個人情報保護方針への同意

梅プランでは項目数を増やしすぎず、応募の心理的ハードルを下げます。

### 処理

```text
入力
↓
入力チェック
↓
Cloudflare Workers
↓
メール送信・外部連携
↓
完了画面
```

### 必須対策

- 必須チェック
- メール形式チェック
- 電話番号チェック
- 文字数チェック
- 不正入力対策
- スパム対策
- 二重送信対策

---

## 17. Tailwind CSS v4ルール

- Utility Classを優先する
- 独自CSSを増やしすぎない
- 同じスタイルを大量にコピーしない
- デザインルールを統一する

特に以下を統一してください。

- コンテンツ最大幅
- 左右余白
- セクション上下余白
- 見出しサイズ
- 本文サイズ
- Primary Red
- Navy
- Sky Blue
- border-radius
- CTAサイズ
- カードの影

梅プランでは、波形・装飾・特殊レイアウトを増やしすぎないでください。

---

## 18. アニメーション（梅プラン・固定）

梅プランのアニメーションは**必要最低限**です。

### 基本方針

> 情報を邪魔しない、最低限の上品なアニメーション。

### 使用してよいもの

- FVの軽いフェードイン
- セクションのfade-up
- ボタンhover
- カードhover
- FAQの開閉
- モバイルメニュー

### 使用しないもの

- staggerによる複雑な段階表示
- image revealの多用
- カウントアップ
- パララックス
- スクロール連動アニメーション
- ページ遷移アニメーション
- GSAP等の外部アニメーションライブラリ
- 文字の回転・斜め移動
- 大きなズーム
- 常時動き続ける装飾

### 基本値

```text
duration: 0.4〜0.7秒
translateY: 12〜20px程度
scale: 原則使用しない
```

### FV

```text
写真: fade-in
コピー: fade-up
サブコピー: fade-up
```

複雑な時間差演出は入れません。

### 各セクション

画面内に入ったら、セクション単位で一度だけfade-upします。

### hover

PCのみ、以下を使用して構いません。

- 背景色変化
- 文字色変化
- 矢印を2〜4px移動

カード自体を大きく浮かせたり、拡大しすぎたりしません。

### 実装方法

- CSSを優先する
- 必要な場合のみIntersectionObserverを使用する
- 外部アニメーションライブラリを追加しない
- `prefers-reduced-motion` に対応する

---

## 19. コメントルール（日本語）

コードには原則として**日本語コメント**を書きます。

以下には必ずコメントを付けます。

- 共通コンポーネント
- ユーティリティ関数
- API処理
- Cloudflare Workers処理
- 応募フォーム処理
- 複雑な条件分岐
- 一見して理由が分からない処理

コメントには「何をしている処理か」と「なぜ必要か」を書きます。

### TypeScript / JavaScript

```ts
/**
 * 応募フォームの入力内容を検証する。
 *
 * Cloudflare Workersへ送信する前に、
 * 必須項目やメール形式に問題がないか確認する。
 */
```

### Astro

```astro
---
/**
 * TOPページの募集職種で表示するデータ。
 * 募集内容の変更時は src/data/jobs.ts を更新する。
 */
import { jobs } from '@/data/jobs';
---
```

---

## 20. TypeScriptルール

- `any` を極力使用しない
- 型名から内容が分かるようにする
- 複雑な型を作りすぎない
- 型の共通化は必要になった段階で行う

```ts
/**
 * 募集職種を表す型。
 */
export interface Job {
  title: string;
  description: string;
  requiredLicense?: string;
  isOpen: boolean;
}
```

---

## 21. 条件分岐・マジックナンバー

- 深いネストを避け、早期リターンを使う
- 複雑な条件は意味の分かる関数へ分離する
- 業務ルール上の意味を持つ数値・文字列は定数として管理する
- 何でもEnum化しない

例：

```text
isValidEntryForm()
shouldShowJob()
hasRequiredLicense()
```

---

## 22. SEO

本サイトは採用目的なので、求人検索を意識した基本SEOを行います。

各ページで次を設定してください。

- `title`
- `description`
- OGP
- canonical
- favicon

募集要項ページでは、職種名や勤務地が検索結果で理解できるタイトルにします。

必要に応じて `JobPosting` 構造化データを検討しますが、募集条件が未確定の状態で勝手に生成しません。

---

## 23. アクセシビリティ

- コントラストを確保する
- ボタン・リンクを十分なサイズにする
- キーボード操作を妨げない
- `alt` を設定する
- 見出し構造を正しく使う
- 色だけで意味を伝えない
- フォームのlabelを省略しない

---

## 24. セキュリティ

初心者による保守を前提とするため、独自実装をなるべく減らします。

以下は原則として独自実装しません。

- 認証
- 暗号化
- Bot対策
- メール送信
- その他セキュリティ処理

Cloudflare等の既存機能を優先してください。

APIキー・応募通知先メールアドレスなどの秘密情報を、コードやリポジトリへ直接書かないでください。
環境変数・Cloudflareのシークレットで管理します。

---

## 25. 変更後の確認（必須）

修正したら、必ず次を実行して成功を確認します。

```bash
pnpm run build
```

加えて、以下を確認します。

- TOPページ
- 会社概要ページ
- 募集要項ページ
- 応募ページ
- グローバルナビゲーション
- 応募CTA
- 応募フォーム
- PC表示
- スマートフォン表示
- リンク切れ
- 画像表示
- 404
- `title`
- `description`
- OGP
- 表示速度

### TOPページで特に確認すること

- FVのコピーが崩れていない
- 写真の人物が不自然に切れていない
- セクション順が正しい
- Primary Redが `#E01B21` からズレていない
- 文字が斜めになっていない
- CTAが目立っている
- 文字量が増えすぎていない
- アニメーションが派手になっていない

---

## 26. 回答・報告のしかた

- 説明は**日本語**で行う
- 何を・なぜ変更したかを初心者に分かる言葉で説明する
- ビルドやテストを実行していない場合は、その旨を明記する
- 失敗した場合は隠さず報告する
- 確信が持てない箇所は断定せず確認を求める
- デザイン変更では、どのセクションを変更したか明記する
- 梅プランの範囲を超える提案を実装した場合は、その理由を明記する

---

## 27. やってはいけないこと（チェックリスト）

- [ ] 既存仕様を勝手に変更する
- [ ] TOPのセクション順を勝手に変更する
- [ ] 依頼範囲を超えて広範囲を書き換える
- [ ] 過度な抽象化・先回り実装をする
- [ ] 不要な下層ページを増やす
- [ ] 不要なJavaScriptを増やす
- [ ] 外部アニメーションライブラリを追加する
- [ ] カウントアップやパララックスを追加する
- [ ] 役割の分からない名前（`Util` `Common` `Box` など）を使う
- [ ] 日本語コメントを書かずに複雑な処理を追加する
- [ ] `src/data/` を使わず、各ページへ求人条件を直接書き込む
- [ ] 給与・休日・従業員数などを複数箇所へ重複記述する
- [ ] Primary Red `#E01B21` を勝手に変更する
- [ ] 文字を斜め・回転させる
- [ ] 読みにくい装飾文字を多用する
- [ ] 応募CTAを目立たなくする
- [ ] 独自の認証・暗号化・Bot対策・メール送信を実装する
- [ ] 秘密情報をコードへ直接書く
- [ ] ビルド確認をせずに完了と報告する

---

## 28. 最後の判断基準

迷った場合は、次の順番で判断してください。

```text
1. 求職者に分かりやすいか
2. 応募につながるか
3. OMGのアットホームな雰囲気が伝わるか
4. 梅プランとして必要十分か
5. 初心者でも安全に更新できるか
6. 表示が速く、スマホで見やすいか
```

> **梅プランは、機能を減らすプランではなく、応募に必要な情報へ絞り込むプラン。**

> **デザイン品質は落とさず、情報量・ページ数・演出量を抑える。**
